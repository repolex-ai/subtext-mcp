{
  "mcpServers": {
    "subtext": {
      "command": "bun",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server.ts"],
      "env": {
        "NODE_PATH": "${CLAUDE_PLUGIN_DATA}/node_modules"
      }
    }
  }
}
